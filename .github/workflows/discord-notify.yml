name: Notify Discord on Push

on:
  push:
    branches:
      - main

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Notify Discord
        uses: Ilshidur/action-discord@master
        env:
          DISCORD_WEBHOOK: ${{ secrets.Code_Alert }}
        with:
          args: |
            --webhook "${{ secrets.Code_Alert }}" 
            "**📦 โปรเจกต์ ${{ github.event.repository.name }}
            **🚀 โค้ดถูก push เข้า branch main โดย ${{ github.actor }}
            📌 โปรดตรวจสอบ [commit](${{ github.event.head_commit.url }})
            <@&1390556493798965308>"
