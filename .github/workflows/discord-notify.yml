name: Notify Discord on Push

on:
  push:
    branches:
      - main

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Notify Discord
        uses: Ilshidur/action-discord@master
        env:
          DISCORD_WEBHOOK: ${{ secrets.Code_Alert }}
        with:
          entryPoint: send
          args: |
            --webhook "${{ secrets.Code_Alert }}" \
            --message "🚀 โค้ดถูก push เข้า branch main โดย **${{ github.actor }}** 🔗 [ดู commit](${{ github.event.head_commit.url }})"